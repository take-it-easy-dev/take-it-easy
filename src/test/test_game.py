import game as game_module
import board as board_module

def test_game():
    game = game_module.Game()
    for field_index in list(range(board_module.NUMBER_OF_FIELDS)):
        # card = (0, 0, 0)
        # game.apply_move()
        assert True
